# Инструкция: Code Review проект с GitHub и WinMerge

## Часть 1: Настройка GitHub репозитория

### Шаг 1: Создание репозитория на GitHub

1. **Зайдите на GitHub** (https://github.com)
2. Нажмите кнопку **"New"** или **"+"** → **"New repository"**
3. Заполните данные:
   - Repository name: `task-manager-code-review`
   - Description: `Демонстрационный проект для code review`
   - Выберите **Public** или **Private**
   - ✅ Отметьте **"Add a README file"**
   - ✅ Добавьте **.gitignore** → выберите **Python**
   - Нажмите **"Create repository"**

### Шаг 2: Клонирование и подготовка проекта

Откройте терминал (Git Bash на Windows):

```bash
# Клонируйте репозиторий
git clone https://github.com/ВАШ_USERNAME/task-manager-code-review.git
cd task-manager-code-review

# Настройте Git (если еще не настроено)
git config user.name "Ваше Имя"
git config user.email "your.email@example.com"
```

### Шаг 3: Добавление файлов проекта

```bash
# Скопируйте все файлы из созданного проекта в этот каталог
# Затем добавьте их в Git:

git add .
git status  # Проверьте, что все файлы добавлены
git commit -m "Initial commit: добавлен код до ревью"
git push origin main
```

---

## Часть 2: Создание веток для Code Review

### Шаг 4: Создание ветки с кодом ДО ревью

```bash
# Создаем ветку для кода до ревью
git checkout -b feature/before-review

# Убедитесь, что в проекте только код из папки before-review
# Создайте файл task_manager.py в корне или в src/

git add .
git commit -m "feat: добавлен код до code review с проблемами"
git push origin feature/before-review
```

### Шаг 5: Создание ветки с кодом ПОСЛЕ ревью

```bash
# Переключаемся на main
git checkout main

# Создаем новую ветку для исправлений
git checkout -b feature/after-review

# Замените task_manager.py на улучшенную версию
# (из папки after-review)

git add .
git commit -m "fix: исправлены все замечания code review"
git push origin feature/after-review
```

### Шаг 6: Создание Pull Request (PR)

1. Зайдите на GitHub в ваш репозиторий
2. Нажмите **"Pull requests"** → **"New pull request"**
3. Настройте:
   - **base:** `main`
   - **compare:** `feature/after-review`
4. Нажмите **"Create pull request"**
5. В описании PR добавьте:

```markdown
## Описание изменений

Исправлены все замечания из code review:

### ✅ Исправлено
- [x] SQL-инъекции (использованы параметризованные запросы)
- [x] Добавлена обработка ошибок (try-except блоки)
- [x] Добавлена валидация входных данных
- [x] Убрано дублирование кода (context manager)
- [x] Рефакторинг в ООП (класс TaskManager)
- [x] Улучшено именование переменных и функций
- [x] Добавлены docstrings и type hints
- [x] Добавлено логирование
- [x] Использованы Enum для статусов

### 📝 Code Review Report
См. [docs/code-review.md](docs/code-review.md)

### 🔍 Сравнение
- Before: `src/before-review/task_manager.py`
- After: `src/after-review/task_manager.py`
```

6. Нажмите **"Create pull request"**

---

## Часть 3: Просмотр изменений на GitHub

### Шаг 7: Использование GitHub для сравнения

1. **В Pull Request:**
   - Перейдите на вкладку **"Files changed"**
   - Вы увидите diff (различия) между версиями
   - Зеленым - добавленный код
   - Красным - удаленный код

2. **Добавление комментариев:**
   - Наведите курсор на строку кода
   - Нажмите на **"+"** слева
   - Добавьте комментарий
   - Нажмите **"Add single comment"**

3. **Создание Code Review:**
   - Нажмите **"Review changes"** справа вверху
   - Выберите:
     - 💬 **Comment** - просто комментарий
     - ✅ **Approve** - одобрить изменения
     - ❌ **Request changes** - требуются изменения
   - Добавьте общий комментарий
   - Нажмите **"Submit review"**

---

## Часть 4: Использование WinMerge

### Шаг 8: Установка WinMerge

1. Скачайте WinMerge: https://winmerge.org/downloads/
2. Установите программу
3. Запустите WinMerge

### Шаг 9: Сравнение файлов в WinMerge

#### Метод 1: Сравнение локальных файлов

```bash
# Сначала получите обе версии файлов

# Версия ДО ревью
git checkout feature/before-review
cp src/before-review/task_manager.py ~/before_task_manager.py

# Версия ПОСЛЕ ревью
git checkout feature/after-review
cp src/after-review/task_manager.py ~/after_task_manager.py
```

**В WinMerge:**
1. **File** → **Open**
2. **Left:** выберите `before_task_manager.py`
3. **Right:** выберите `after_task_manager.py`
4. Нажмите **OK**

#### Метод 2: Сравнение папок

1. **File** → **Open**
2. **Left:** выберите папку `src/before-review/`
3. **Right:** выберите папку `src/after-review/`
4. Отметьте ✅ **Include subfolders**
5. Нажмите **OK**

### Шаг 10: Работа с WinMerge

**Панель инструментов:**
- 🔽 **Next Difference** - следующее различие
- 🔼 **Previous Difference** - предыдущее различие
- ▶️ **Copy to Right** - копировать изменение вправо
- ◀️ **Copy to Left** - копировать изменение влево

**Цветовая схема:**
- 🟨 **Желтый** - строки различаются
- 🟩 **Зеленый** - строки добавлены справа
- ⬜ **Серый** - строки удалены справа

**Создание отчета:**
1. **Tools** → **Generate Report**
2. Выберите формат: **HTML** или **Simple HTML**
3. Сохраните отчет
4. Откройте в браузере

### Шаг 11: Сравнение веток через Git

```bash
# Сравнить две ветки
git diff feature/before-review feature/after-review

# Сравнить конкретный файл
git diff feature/before-review:task_manager.py feature/after-review:task_manager.py

# Сохранить diff в файл
git diff feature/before-review feature/after-review > changes.diff

# Создать patch файл
git diff feature/before-review feature/after-review > improvements.patch
```

**Открыть diff в WinMerge:**
```bash
# Создайте временные файлы
git show feature/before-review:task_manager.py > temp_before.py
git show feature/after-review:task_manager.py > temp_after.py

# Затем откройте их в WinMerge
```

---

## Часть 5: Расширенные возможности (для более высокой оценки)

### Дополнение 1: GitHub Actions для автоматической проверки

Создайте файл `.github/workflows/code-quality.yml`:

```yaml
name: Code Quality Check

on:
  pull_request:
    branches: [ main ]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
    - name: Install dependencies
      run: |
        pip install pylint black mypy
    - name: Run Black
      run: black --check src/
    - name: Run Pylint
      run: pylint src/after-review/
    - name: Run MyPy
      run: mypy src/after-review/
```

### Дополнение 2: Использование GitHub Projects

1. В репозитории: **Projects** → **New project**
2. Создайте колонки:
   - 📋 **To Review** - на ревью
   - 🔍 **In Review** - в процессе
   - ✅ **Reviewed** - проверено
   - ❌ **Needs Changes** - требует изменений
3. Добавьте задачи из PR

### Дополнение 3: Защита веток (Branch Protection)

1. **Settings** → **Branches**
2. **Add rule**
3. Branch name pattern: `main`
4. Включите:
   - ✅ Require pull request reviews before merging
   - ✅ Require status checks to pass
   - ✅ Require conversation resolution before merging

### Дополнение 4: GitLab CI/CD (если используете GitLab)

Создайте `.gitlab-ci.yml`:

```yaml
stages:
  - test
  - quality

code_quality:
  stage: quality
  image: python:3.11
  script:
    - pip install pylint black
    - black --check src/
    - pylint src/after-review/
  only:
    - merge_requests
```

---

## Часть 6: Презентация результатов

### Что показать преподавателю:

1. **GitHub репозиторий:**
   - Две ветки (before/after)
   - Pull Request с описанием
   - Code Review комментарии
   - Diff на вкладке "Files changed"

2. **WinMerge скриншоты:**
   - Сравнение файлов side-by-side
   - Highlighted различия
   - Отчет в HTML

3. **Документация:**
   - `docs/code-review.md` - отчет о ревью
   - README.md с описанием проекта

4. **Демонстрация улучшений:**
   - До: SQL-инъекции, плохой код
   - После: безопасно, читаемо, документировано

---

## Чек-лист для защиты проекта

- [ ] Репозиторий на GitHub создан
- [ ] Код до ревью загружен в отдельную ветку
- [ ] Код после ревью в другой ветке
- [ ] Pull Request создан
- [ ] Документ code-review.md заполнен
- [ ] WinMerge установлен и настроен
- [ ] Сделаны скриншоты сравнения
- [ ] README.md описывает проект
- [ ] Можете объяснить каждое исправление

---

## Полезные команды Git

```bash
# Просмотр истории коммитов
git log --oneline --graph --all

# Просмотр изменений
git diff

# Откат изменений
git checkout -- <file>

# Обновление из remote
git pull origin main

# Создание тега
git tag -a v1.0 -m "Version after review"
git push origin v1.0

# Просмотр веток
git branch -a

# Удаление ветки
git branch -d feature/old-branch
```

---

## Критерии оценки (ожидаемые)

| Критерий | Баллы |
|----------|-------|
| Код до ревью с проблемами | 10 |
| Документ code review | 15 |
| Исправленный код | 20 |
| GitHub репозиторий + PR | 20 |
| WinMerge сравнение | 15 |
| Дополнительно (CI/CD, тесты) | +20 |
| **Итого** | **100+** |

Удачи с проектом! 🚀
